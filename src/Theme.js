const { createMuiTheme } = require("@material-ui/core");

export const theme = createMuiTheme({
    palette: {
      darkBlue: '#0a263f',
      button: '#27d6ff',
      buttonHover: '#12a5c7',
      white: 'white',
      inputBorder: '#3f51b5',
      message: '#124f868c'
    },
  });