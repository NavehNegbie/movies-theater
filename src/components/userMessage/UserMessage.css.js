const styles = theme => ({
    text: {
        backgroundColor: theme.palette.message,
        width: '42%',
        margin: 'auto',
        marginTop: '100px',
        padding: '30px'
    }
});

export default styles;
