const styles = theme => ({
    appBar: {
        backgroundColor: theme.palette.darkBlue,
        alignItems: 'center'
    },
    link: {
        textDecoration: 'none',
        color: theme.palette.white,
        cursor: 'pointer'
    }
  });

  export default styles;
  