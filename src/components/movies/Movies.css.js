const styles = theme => ({
    moviesContainer: {
        width: '100%',
        margin: 0,
        maxHeight: 'calc(86vh - 90px)',
        overflow: 'auto',
    },
    link: {
        textDecoration: "none"
    },
  });

  export default styles;