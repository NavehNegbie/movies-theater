const styles = theme => ({
    button: {
        backgroundColor: theme.palette.button,
        '&:hover': {
            backgroundColor: theme.palette.buttonHover,
        },
    }
  });

  export default styles;
  